import { RouterConfig } from '@angular/router';
import { CBConfigComponent } from './component/cbconfig.component';

export const cbConfigRoutes: RouterConfig = [
    {
        path: 'cbconfig',
        component: CBConfigComponent
    }
];
