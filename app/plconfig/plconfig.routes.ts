import { Routes } from '@angular/router';

import { PLConfigComponent } from './component/plconfig.component';

export const plconfigRoutes: Routes = [
    {
        path: 'plconfig',
        component: PLConfigComponent
    }
];
